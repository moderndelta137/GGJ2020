using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EndingCharacterController : MonoBehaviour
{

    //private endingFlag;
    private const int LOSE = 0;
    private const int PLAYER1WIN = 1;
    private const int PLAYER2WIN = 2;
    private const int TRUEEND = 3;

    // Start is called before the first frame update
    void Start()
    {
        /*
        endingFlag = 1; 

        switch(endingFlag){
            case LOSE:
                
                break;
        }*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
